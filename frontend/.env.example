# OAuth 2.0 Client ID from Google Cloud Console — the same public client ID
# configured as GOOGLE_CLIENT_ID on the backend. Public by design (it ends up
# embedded in this bundle), not a secret.
VITE_GOOGLE_CLIENT_ID=
