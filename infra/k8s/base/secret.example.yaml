# Template only — do NOT commit the real secret. Create it directly in the
# cluster instead, e.g.:
#   kubectl create secret generic backend-secret \
#     --from-literal=MONGO_URI='mongodb://user:pass@host:27017/db?authSource=admin'
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
type: Opaque
stringData:
  MONGO_URI: 'mongodb://<user>:<password>@<host>:27017/digital_ai_project?authSource=admin'
