apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - backend-deployment.yaml
  - backend-service.yaml
  - frontend-deployment.yaml
  - frontend-service.yaml
  - rss-collect-cronjob.yaml
  - ingress.yaml
  # secret.example.yaml is a template, not applied — create the real Secret
  # directly in the cluster (kubectl create secret ..., see the file's header).
